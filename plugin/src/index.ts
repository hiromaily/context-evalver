/**
 * context-optimizer — Claude Code plugin entry point.
 * Subsequent tasks wire hook handlers, commands, and the IPC client into this module.
 */

export const PLUGIN_NAME = 'context-optimizer' as const;
export const PLUGIN_VERSION = '0.1.0' as const;
